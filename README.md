# Random Color Role Bot

This is a Discord bot I made to help me update the color of a role every day. The `/api/index.py` is so I can deploy it to Vercel serverless functions and trigger it via cron job.
